{% extends "@Institucional/Template/template.html.twig"%}
{% block title %}{{ title}}{% endblock %}
{% block stylesheets %}<style>body{background-color: #ECF0F5 !important;}</style>{% endblock %}
{% block body %}
    <!-- Main content -->
    <section class="content">
        <div class="error-page">
          <h2 class="headline text-success"> <i class="fa fa-check text-success"></i></h2>

          <div class="error-content">
            <h3><i class="fa fa-thumbs-o-up text-success"></i> {{ header }}</h3>

            <p class="text-justify text-uppercase">{{ message }}</p>
            <p>{{ instruction }}</p>
            <p>
                Você pode retornar a
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path("dashboard") }}">Dashboard</a>
                {% else %}
                    <a href="{{ path("homepage") }}">Home</a>
                {% endif %}
                .
            </p>
          </div>
          <!-- /.error-content -->
        </div>
        <!-- /.error-page -->
    </section>
    <!-- /.content -->
{% endblock %}
